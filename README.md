The Voice Calculator is a program written in WPF with C# with XAML, using Windows Speech Recognition API.

1. It is a simple calculator with 2 digits, that user can modify, 
and then user needs to pronounce the sum of the digits in order for the sum to appear in the UI.

2. When the user clicks and holds on any element, the user reads its text aloud and the program recognizes the speech.



